# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files with secrets (e.g. Azure OpenAI key). Never commit these.
.env
.env.*
# ...but DO commit the placeholder template.
!.env.example

# Internal docs & planning — keep everything except the public README out of git.
*.md
!README.md
tasks/

# Local-only real example (kept on disk, never committed).
project background.local.txt
*.local.txt

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
